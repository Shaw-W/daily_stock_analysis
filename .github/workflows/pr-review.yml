# PR 自动审查已禁用。
# 保留手动入口用于在 Actions 页面显示禁用状态；PR 创建或更新不会触发本 workflow。

name: PR Review

on:
  workflow_dispatch:

jobs:
  disabled:
    name: PR Review Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "PR Review workflow is disabled."
